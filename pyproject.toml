[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "companies-house-retrieval"
version = "0.1.0"
description = "Companies House data retrieval and analysis"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]